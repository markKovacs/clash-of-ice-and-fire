package com.coinf.repository;

import com.coinf.entity.blueprint.StructureBonus;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StructureBonusRepository extends JpaRepository<StructureBonus, Long> {
}
