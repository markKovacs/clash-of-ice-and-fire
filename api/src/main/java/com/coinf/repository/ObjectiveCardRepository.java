package com.coinf.repository;

import com.coinf.entity.blueprint.ObjectiveCard;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ObjectiveCardRepository extends JpaRepository<ObjectiveCard, Long> {
}
